import * as React  from 'react';
import '../../assets/scss/Card.scss';

const Card: React.FC = () => (
  <div className="Card" data-testid="Card">
    Card Component
  </div>
);

export default Card;
